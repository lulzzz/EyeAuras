<native:TransparentWindow x:Class="EyeAuras.UI.RegionSelector.Views.RegionSelectorWindow"
                          xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                          xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                          xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                          xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                          xmlns:overlay="clr-namespace:EyeAuras.UI.Overlay"
                          xmlns:native="clr-namespace:PoeShared.Native;assembly=PoeShared"
                          xmlns:eye="http://coderush.net/poeeye/"
                          xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
                          xmlns:eyeAuras="clr-namespace:EyeAuras;assembly=EyeAuras"
                          xmlns:viewModels="clr-namespace:EyeAuras.UI.Overlay.ViewModels"
                          xmlns:viewModels1="clr-namespace:EyeAuras.UI.RegionSelector.ViewModels"
                          mc:Ignorable="d"
                          Title="RegionSelector" 
                          ResizeMode="NoResize"
                          SizeToContent="Manual"
                          WindowStyle="None"
                          WindowStartupLocation="CenterOwner"
                          Topmost="True"
                          Background="{x:Null}"
                          BorderBrush="{x:Null}"
                          AllowsTransparency="True"
                          DataContext="{Binding}"
                          FontFamily="{StaticResource FontPlayRegular}"
                          ShowActivated="True"
                          WindowState="Maximized" 
                          d:DataContext="{d:DesignInstance viewModels1:RegionSelectorViewModel}">
    <Grid x:Name="RegionSelectorRoot">
        <Border 
                Background="{StaticResource OverlayWindowTitleBackgroundBrushKey}"
                Opacity="0.5"
                BorderBrush="{StaticResource OverlayWindowBorderBrushKey}"
                BorderThickness="1">
        </Border>
        <Canvas>
            <Rectangle x:Name="RegionCandidate" Stroke="Red" StrokeThickness="1"/>
        </Canvas>
        <TextBlock VerticalAlignment="Center"
                   FontSize="20"
                   Foreground="Aqua"
                   HorizontalAlignment="Center">
            <TextBlock.Inlines>
                <Run Text="{Binding MouseRegion.Window.Title, Mode=OneWay}"/> : 
                WindowRect = <Run Text="{Binding MouseRegion.Window.WindowBounds, Mode=OneWay}"/>
                Selection = <Run Text="{Binding MouseRegion.Selection, Mode=OneWay}"/>
                
            </TextBlock.Inlines>
        </TextBlock>
    </Grid>


</native:TransparentWindow>